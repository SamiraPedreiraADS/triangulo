#include <iostream>
using namespace std;
int main() {
  int lado1, lado2, lado3;
  int triangulo, soma, esc, iso, equi;
  
  cout << "Informe uma medida, em metros: " ;
  cin >> lado1;

  cout << "Informe uma segunda medida em metros: ";
  cin >> lado2;

  cout << "Informe uma segunda medida em metros: ";
  cin >> lado3;

  if (lado1 < lado2 + lado3) {
    cout << "Essas medidas forma uma triângulo!";
  }
   if (lado2 < lado1 + lado3) {
    
   if (lado3 < lado2 + lado1) {
  }
